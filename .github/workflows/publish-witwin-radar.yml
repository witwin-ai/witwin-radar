name: Build and publish witwin-radar

on:
  push:
    branches:
      - main
      - master
      - "codex/**"
  pull_request:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  validate_release:
    if: github.event_name != 'release' || (startsWith(github.event.release.tag_name, 'witwin-radar-v') && !github.event.release.prerelease)
    name: Validate release
    runs-on: ubuntu-latest
    outputs:
      package_dir: ${{ steps.package_dir.outputs.path }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Resolve package directory
        id: package_dir
        shell: bash
        run: |
          if [ -f pyproject.toml ]; then
            echo "path=." >> "$GITHUB_OUTPUT"
          elif [ -f radar/pyproject.toml ]; then
            echo "path=radar" >> "$GITHUB_OUTPUT"
          else
            echo "Unable to find pyproject.toml for the witwin-radar package." >&2
            exit 1
          fi

      - name: Validate release tag and package version
        if: github.event_name == 'release'
        shell: bash
        env:
          PACKAGE_DIR: ${{ steps.package_dir.outputs.path }}
          RELEASE_TAG: ${{ github.event.release.tag_name }}
        run: |
          python - <<'PY'
          import os
          import re
          import sys
          import tomllib
          from pathlib import Path

          tag = os.environ["RELEASE_TAG"]
          match = re.fullmatch(r"witwin-radar-v(?P<version>[0-9A-Za-z.+_-]+)", tag)
          if match is None:
              print(f"Release tag '{tag}' must match 'witwin-radar-v<version>'.", file=sys.stderr)
              raise SystemExit(1)

          expected_version = match.group("version")
          pyproject_path = Path(os.environ["PACKAGE_DIR"]) / "pyproject.toml"
          data = tomllib.loads(pyproject_path.read_text(encoding="utf-8"))

          project = data["project"]
          package_name = project["name"]
          package_version = project["version"]

          if package_name != "witwin-radar":
              print(f"Expected package name 'witwin-radar', found '{package_name}'.", file=sys.stderr)
              raise SystemExit(1)

          if package_version != expected_version:
              print(
                  f"Release tag version '{expected_version}' does not match pyproject version '{package_version}'.",
                  file=sys.stderr,
              )
              raise SystemExit(1)

          print(f"Validated {package_name} {package_version} from {pyproject_path}.")
          PY

  build_cuda_wheels:
    name: Build stable CUDA wheel / ${{ matrix.os }}
    needs: validate_release
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-22.04, windows-2022]

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Free disk space (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          echo "Disk before cleanup:"; df -h /
          sudo rm -rf /usr/share/dotnet /opt/ghc /usr/local/lib/android \
            /usr/local/share/boost /opt/hostedtoolcache/CodeQL \
            /usr/local/share/powershell /usr/share/swift || true
          sudo docker image prune --all --force || true
          echo "Disk after cleanup:"; df -h /

      - name: Install CUDA Toolkit
        if: runner.os == 'Windows'
        uses: Jimver/cuda-toolkit@v0.2.35
        with:
          cuda: "12.8.1"
          method: local
          log-file-suffix: "${{ runner.os }}-stable-abi"

      - name: Set up MSVC
        if: runner.os == 'Windows'
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64

      - name: Export Windows CUDA_HOME
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $cudaRoot = $env:CUDA_PATH
          if (-not $cudaRoot) {
            $nvcc = (Get-Command nvcc -ErrorAction Stop).Source
            $cudaRoot = Split-Path (Split-Path $nvcc -Parent) -Parent
          }
          "CUDA_HOME=$cudaRoot" >> $env:GITHUB_ENV
          "CUDA_PATH=$cudaRoot" >> $env:GITHUB_ENV
          "CUDA_ROOT=$cudaRoot" >> $env:GITHUB_ENV
          "CUDA_BIN_PATH=$cudaRoot" >> $env:GITHUB_ENV

      - name: Show toolchain
        if: runner.os == 'Windows'
        shell: bash
        run: |
          python --version
          nvcc --version
          command -v cl || true

      - name: Install CUDA extension build dependencies
        run: |
          python -m pip install --upgrade pip setuptools wheel ninja numpy
          python -m pip install torch==2.10.0 --index-url https://download.pytorch.org/whl/cu128
          python -m pip install build

      - name: Build packaged stable Dirichlet CUDA extension
        if: runner.os == 'Windows'
        shell: bash
        env:
          MAX_JOBS: "2"
          WITWIN_CUDA_GENCODE_ARCHES: "7.0;7.5;8.0;8.6;8.7;8.9;9.0;10.0;10.1;12.0+PTX"
          PACKAGE_DIR: ${{ needs.validate_release.outputs.package_dir }}
          WITWIN_RADAR_DIRICHLET_CUDA_BUILD_DIR: ${{ runner.temp }}/_radar_native/stable_abi_v1
        run: |
          python "$PACKAGE_DIR"/scripts/build_radar_cuda_prebuilt.py --verbose

      - name: Build manylinux_2_28 wheel
        if: runner.os == 'Linux'
        uses: pypa/cibuildwheel@v3.3.0
        env:
          CIBW_BUILD: cp310-manylinux_x86_64
          CIBW_MANYLINUX_X86_64_IMAGE: manylinux_2_28
          CIBW_BEFORE_ALL_LINUX: >-
            dnf install -y dnf-plugins-core gcc-toolset-12 &&
            rpm --import https://developer.download.nvidia.com/compute/cuda/repos/rhel8/x86_64/D42D0685.pub &&
            dnf config-manager --add-repo https://developer.download.nvidia.com/compute/cuda/repos/rhel8/x86_64/cuda-rhel8.repo &&
            dnf clean expire-cache &&
            dnf install -y cuda-toolkit-12-8 &&
            ln -sfn /usr/local/cuda-12.8 /usr/local/cuda &&
            /usr/local/cuda/bin/nvcc --version
          CIBW_ENVIRONMENT_LINUX: >-
            CUDA_HOME=/usr/local/cuda
            CUDA_PATH=/usr/local/cuda
            CUDA_ROOT=/usr/local/cuda
            CUDA_BIN_PATH=/usr/local/cuda
            CUDACXX=/usr/local/cuda/bin/nvcc
            CUDAHOSTCXX=/opt/rh/gcc-toolset-12/root/usr/bin/g++
            PATH=/usr/local/cuda/bin:$PATH
            LD_LIBRARY_PATH=/usr/local/cuda/lib64:$LD_LIBRARY_PATH
            MAX_JOBS=2
            WITWIN_CUDA_GENCODE_ARCHES="7.0;7.5;8.0;8.6;8.7;8.9;9.0;10.0;10.1;12.0+PTX"
          CIBW_BEFORE_BUILD_LINUX: >-
            pip install --upgrade pip setuptools wheel ninja numpy build &&
            pip install torch==2.10.0 --index-url https://download.pytorch.org/whl/cu128 &&
            python {project}/scripts/build_radar_cuda_prebuilt.py --verbose &&
            python {project}/scripts/verify_cuda_binary_arches.py
            --stem _radar_native
            {project}/witwin/radar/cuda/prebuilt
          CIBW_REPAIR_WHEEL_COMMAND_LINUX: >-
            auditwheel repair --plat manylinux_2_28_x86_64
            --wheel-dir {dest_dir}
            --exclude libc10.so --exclude libc10_cuda.so
            --exclude libtorch.so --exclude libtorch_cpu.so
            --exclude libtorch_cuda.so --exclude libtorch_python.so
            --exclude libcudart.so.12 --exclude libcuda.so.1
            {wheel}
        with:
          package-dir: ${{ needs.validate_release.outputs.package_dir }}
          output-dir: ${{ needs.validate_release.outputs.package_dir }}/dist

      - name: Verify compiled CUDA architectures
        if: runner.os == 'Windows'
        shell: bash
        env:
          PACKAGE_DIR: ${{ needs.validate_release.outputs.package_dir }}
        run: |
          python "$PACKAGE_DIR"/scripts/verify_cuda_binary_arches.py \
            --stem _radar_native \
            "$PACKAGE_DIR"/witwin/radar/cuda/prebuilt

      - name: Build platform wheel
        if: runner.os == 'Windows'
        shell: bash
        env:
          PACKAGE_DIR: ${{ needs.validate_release.outputs.package_dir }}
        run: |
          python -m build --wheel "$PACKAGE_DIR"

      - name: Smoke install prebuilt wheel
        shell: bash
        env:
          PACKAGE_DIR: ${{ needs.validate_release.outputs.package_dir }}
          WITWIN_RADAR_DIRICHLET_CUDA_PREBUILT: "1"
          WITWIN_RADAR_DIRICHLET_CUDA_BUILD_DIR: ${{ runner.temp }}/missing_prebuilt_build_dir
        run: |
          python -m pip uninstall -y witwin-radar || true
          python -m pip install --no-deps "$PACKAGE_DIR"/dist/*.whl
          python - <<'PY'
          import importlib.util
          import os
          import site
          import zipfile
          from pathlib import Path

          wheel = next((Path(os.environ["PACKAGE_DIR"]) / "dist").glob("*.whl"))
          names = zipfile.ZipFile(wheel).namelist()
          native = [name for name in names if "_radar_native" in name and name.endswith((".so", ".pyd"))]
          assert len(native) == 1, native
          assert "/prebuilt/" in f"/{native[0]}"
          assert "torch_" not in native[0]

          candidates = []
          for root in site.getsitepackages() + [site.getusersitepackages()]:
              candidates.append(Path(root) / "witwin" / "radar" / "cuda" / "build.py")
          build_path = next((path for path in candidates if path.exists()), None)
          assert build_path is not None, f"installed build.py not found in {candidates}"

          spec = importlib.util.spec_from_file_location("_radar_native_build", build_path)
          module = importlib.util.module_from_spec(spec)
          spec.loader.exec_module(module)

          path = module.prebuilt_extension_path()
          assert path.exists(), f"missing packaged prebuilt extension: {path}"
          assert path.parent.name == "prebuilt"
          extension = module.build_extension()
          print(f"Loaded packaged radar Dirichlet CUDA extension from {extension.__file__}")
          PY

      - name: Upload wheel artifact
        uses: actions/upload-artifact@v4
        with:
          name: wheel-${{ matrix.os }}
          path: ${{ needs.validate_release.outputs.package_dir }}/dist/*.whl
          if-no-files-found: error

  test_torch_compatibility:
    name: Stable ABI / ${{ matrix.os }} / py${{ matrix.compatibility.python_version }} / torch${{ matrix.compatibility.torch_version }}
    needs:
      - validate_release
      - build_cuda_wheels
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-22.04, windows-2022]
        compatibility:
          - {python_version: "3.10", torch_version: "2.10.0", cuda_index: "cu128"}
          - {python_version: "3.11", torch_version: "2.10.0", cuda_index: "cu128"}
          - {python_version: "3.12", torch_version: "2.10.0", cuda_index: "cu128"}
          - {python_version: "3.13", torch_version: "2.10.0", cuda_index: "cu128"}
          - {python_version: "3.14", torch_version: "2.10.0", cuda_index: "cu128"}
          - {python_version: "3.14", torch_version: "2.11.0", cuda_index: "cu128"}
          - {python_version: "3.14", torch_version: "2.12.0", cuda_index: "cu126"}
          - {python_version: "3.14", torch_version: "2.13.0", cuda_index: "cu126"}

    steps:
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.compatibility.python_version }}

      - name: Download stable wheel
        uses: actions/download-artifact@v4
        with:
          name: wheel-${{ matrix.os }}
          path: dist

      - name: Install compatibility environment
        shell: bash
        run: |
          python -m pip install --upgrade pip
          python -m pip install --no-cache-dir "torch==${{ matrix.compatibility.torch_version }}" \
            --index-url "https://download.pytorch.org/whl/${{ matrix.compatibility.cuda_index }}"
          python -m pip install --no-deps dist/*.whl

      - name: Load the same stable binary
        shell: bash
        env:
          EXPECTED_TORCH: ${{ matrix.compatibility.torch_version }}
          WITWIN_RADAR_DIRICHLET_CUDA_PREBUILT: "1"
          WITWIN_RADAR_DIRICHLET_CUDA_BUILD_DIR: ${{ runner.temp }}/must-not-jit-build
        run: |
          python - <<'PY'
          import importlib.util
          import os
          import site
          from pathlib import Path

          import torch

          assert torch.__version__.split("+", 1)[0] == os.environ["EXPECTED_TORCH"]
          candidates = [
              Path(root) / "witwin" / "radar" / "cuda" / "build.py"
              for root in site.getsitepackages() + [site.getusersitepackages()]
          ]
          build_path = next((path for path in candidates if path.exists()), None)
          assert build_path is not None, candidates
          spec = importlib.util.spec_from_file_location("_radar_native_build", build_path)
          module = importlib.util.module_from_spec(spec)
          spec.loader.exec_module(module)

          native_path = module.prebuilt_extension_path()
          assert native_path.exists(), native_path
          extension = module.build_extension()
          assert Path(extension.__file__).resolve() == native_path.resolve()
          assert extension.forward_chunked is not None
          print(f"Loaded {native_path.name} with torch {torch.__version__} on Python {os.sys.version.split()[0]}")
          PY

  build_sdist:
    name: Build source distribution
    needs: validate_release
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Build distributions
        shell: bash
        env:
          PACKAGE_DIR: ${{ needs.validate_release.outputs.package_dir }}
        run: |
          python -m pip install --upgrade pip build
          python -m build --sdist "$PACKAGE_DIR"

      - name: Upload source distribution artifact
        uses: actions/upload-artifact@v4
        with:
          name: sdist
          path: ${{ needs.validate_release.outputs.package_dir }}/dist/*.tar.gz
          if-no-files-found: error

  publish:
    name: Publish to PyPI
    if: github.event_name == 'release'
    needs:
      - build_cuda_wheels
      - test_torch_compatibility
      - build_sdist
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    environment:
      name: pypi
      url: https://pypi.org/project/witwin-radar/

    steps:
      - name: Download wheel artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: wheel-*
          path: dist
          merge-multiple: true

      - name: Download source distribution artifact
        uses: actions/download-artifact@v4
        with:
          name: sdist
          path: dist

      - name: Publish distributions to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist
          skip-existing: true
